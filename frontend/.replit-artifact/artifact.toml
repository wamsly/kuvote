kind = "web"
  previewPath = "/"
  title = "KUVOTE"
  version = "1.0.0"
  id = "artifacts/kuvote"
  router = "path"

  [[integratedSkills]]
  name = "react-vite"
  version = "1.0.0"

  [[services]]
  name = "web"
  paths = [ "/" ]
  localPort = 3000

  [services.development]
  run = "pnpm --filter @workspace/kuvote run dev"

  [services.production]
  build = [ "pnpm", "--filter", "@workspace/kuvote", "run", "build" ]
  publicDir = "kuvote/frontend/dist/public"
  serve = "static"

  [[services.production.rewrites]]
  from = "/*"
  to = "/index.html"

  [services.env]
  PORT = "3000"
  BASE_PATH = "/"
  